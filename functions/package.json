{
  "name": "brush-quest-functions",
  "description": "Firebase Cloud Functions for Brush Quest — Apple SIWA token revocation per Apple Guideline 5.1.1(v).",
  "main": "src/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "eslint --ext .js src/",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  },
  "private": true
}
